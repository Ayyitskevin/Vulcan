schema_version = 2

[server]
host = "127.0.0.1"
port = 8140
log_level = "INFO"

[providers.deterministic]
type = "deterministic"
response_text = "vulcan-deterministic-response"

[[models]]
id = "vulcan-test"
provider = "deterministic"
provider_model = "vulcan-test"
capabilities = ["chat"]
description = "Deterministic no-I/O test model"
